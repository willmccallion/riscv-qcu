//! Hardware driver modules for firmware peripherals.
//!
//! Contains drivers for hardware accelerators and other peripherals accessed
//! via memory-mapped I/O. Each driver module provides a safe interface to
//! its corresponding hardware component.

pub mod accelerator;
